@model IEnumerable<CarsManage.Models.Cars_Brand>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Brand_nm)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Brand_nm)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Brand_no }) |
            @Html.ActionLink("Details", "Details", new { id=item.Brand_no }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Brand_no })
        </td>
    </tr>
}

</table>
