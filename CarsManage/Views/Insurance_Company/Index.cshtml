@model IEnumerable<CarsManage.Models.Insurance_Company>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Company_nm)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Company_nm)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Company_no }) |
            @Html.ActionLink("Details", "Details", new { id=item.Company_no }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Company_no })
        </td>
    </tr>
}

</table>
